export const components = () => ({
  Badge: {
    styles: (theme, params, { variant }) => ({}),
  },
  Button: {
    styles: (theme, params, { variant }) => ({}),
  },
  Container: {
    defaultProps: {
      sizes: {
        xs: 540,
        sm: 720,
        md: 960,
        lg: 1500,
        xl: 1500,
      },
    },
    styles: (theme, props) => ({}),
  },
  Drawer: {
    styles: (theme) => ({}),
  },
  Menu: {
    styles: (theme) => ({}),
  },
  Pagination: {
    styles: (theme, params, { variant }) => ({}),
  },
  Paper: {
    styles: (theme) => ({}),
  },
  Text: {
    styles: (theme) => ({}),
  },
  Title: {
    styles: (theme) => ({}),
  },
  SegmentedControl: {
    styles: (theme) => ({}),
  },
  Select: {
    styles: (theme) => ({}),
  },
});
